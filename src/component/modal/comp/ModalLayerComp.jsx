import React, {} from 'react';
import { ModalLayer } from '../style/ModalStyle';

export const ModalLayerComp = (props) =>{
    return(
        <ModalLayer layerDisplay={props.layerDisplay}></ModalLayer>
    )
}